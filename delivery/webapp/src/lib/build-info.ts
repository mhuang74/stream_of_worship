// Fallback build stamp for non-git/dev environments. Overwritten by
// scripts/generate-build-info.ts during `next build` (prebuild).
export const BUILD_COMMIT_HASH = "dev";
export const BUILD_COMMIT_DATE = "";
